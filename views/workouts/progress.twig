{% extends "index.twig" %}
{% block content %}
    <h1>Workout Summary</h1>
    <div class="pull-left">
        <button class="summary btn btn-default btn-sm">Summary</button>
        <div class="btn-group">
            <button type="button" class="btn btn-default btn-sm">By Type</button>
            <button type="button" class="btn btn-default btn-sm dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <span class="caret"></span>
                <span class="sr-only">Toggle Dropdown</span>
            </button>
            <ul class="dropdown-menu">
                <li><a class="by-type" href="#">View All</a></li>
                <li role="separator" class="divider"></li>
                {% for type in types %}
                    <li><a class="by-type" href="#" id="{{ type }}">{{ type }}</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>
    <canvas class="ct-chart"></canvas>
    <script type="text/javascript" src="{{ app_path }}/assets/js/workout-summary.js"></script>
{% endblock %}